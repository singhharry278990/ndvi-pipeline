# NDVI pipeline — Python 3.9–3.12
# Inference (Notebook 3) does not need the full Presto training stack.
#
# CPU:
#   pip install -r requirements.txt
# GPU (CUDA 12.1 example — pick the wheel that matches the server CUDA):
#   pip install -r requirements.txt
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Ubuntu system packages (rasterio/GDAL):
#   sudo apt-get update
#   sudo apt-get install -y gdal-bin libgdal-dev python3-dev build-essential

# --- Notebook 3: whole-AOI inference ---
torch>=2.0,<2.6
numpy>=1.23,<2.0
einops>=0.6
rasterio>=1.3
matplotlib>=3.6
pyproj>=3.4

# --- Notebook 0: Earth Engine export (straight to local disk, no Drive) ---
earthengine-api>=0.1.386
geemap>=0.32
geedim>=1.8
